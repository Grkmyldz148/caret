node_modules/
dist/
build/
*.log
.DS_Store
.env
.env.*
!.env.example
*.tsbuildinfo
coverage/
.turbo/
.cache/
.pnpm-store/
.mcp.json

# Caret image-to-art caches (regenerable)
*.cache.txt
*.caret-cache.txt
**/.*.caret-cache.txt

# Locally-installed Claude Code skills (npx skills add) — not part of
# this repo. Each developer manages their own skill set.
.agents/

# `prepack` copies the workspace registry + README + LICENSE into
# packages/caret/ for the published tarball — never commit those copies.
packages/caret/registry/
packages/caret/README.md
packages/caret/LICENSE

